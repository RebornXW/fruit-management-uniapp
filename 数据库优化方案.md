# 水果档口管理系统数据库优化方案

## 1. 创建中间表实现多对多关系

### 1.1 付款与销售记录多对多关系

目前系统中，付款记录(payment_records)表与销售记录(sales_records)表是一对一关系，即一笔付款只能对应一条销售记录。这种设计存在以下问题：

1. 客户可能一次性付款结算多笔销售记录
2. 一笔销售记录可能分多次付款
3. 无法准确追踪每笔销售记录的实际付款情况

因此，需要创建中间表实现多对多关系：

#### payment_sales_relation表设计

| 字段名 | 类型 | 描述 | 约束 |
|--------|------|------|------|
| id | INT | 关系ID | 主键, 自增 |
| payment_id | INT | 付款记录ID | 外键(payment_records.id), 非空 |
| sales_id | INT | 销售记录ID | 外键(sales_records.id), 非空 |
| amount | DECIMAL(12,2) | 分配到该销售记录的付款金额 | 非空 |
| created_at | DATETIME | 创建时间 | 非空 |
| updated_at | DATETIME | 更新时间 | 非空 |

#### 修改payment_records表

移除sales_record_id字段，因为一笔付款可能对应多条销售记录。

```sql
ALTER TABLE payment_records DROP COLUMN sales_record_id;
```

#### API调整

1. 创建付款记录时，允许指定多个销售记录ID及对应的金额分配
2. 查询付款记录时，返回关联的所有销售记录信息
3. 查询销售记录时，返回关联的所有付款记录信息

## 2. 合并日期和时间字段

目前系统中，多个表使用了分开的日期(DATE)和时间(TIME)字段，如：

- sales_records表: sale_date + sale_time
- payment_records表: payment_date + payment_time
- inventory_records表: operation_date + operation_time
- operation_records表: operation_date + operation_time

这种设计不利于按时间范围查询，建议合并为单一的DATETIME字段：

### 2.1 修改sales_records表

```sql
ALTER TABLE sales_records ADD COLUMN sale_datetime DATETIME;
UPDATE sales_records SET sale_datetime = CONCAT(sale_date, ' ', sale_time);
ALTER TABLE sales_records DROP COLUMN sale_date, DROP COLUMN sale_time;
```

### 2.2 修改payment_records表

```sql
ALTER TABLE payment_records ADD COLUMN payment_datetime DATETIME;
UPDATE payment_records SET payment_datetime = CONCAT(payment_date, ' ', payment_time);
ALTER TABLE payment_records DROP COLUMN payment_date, DROP COLUMN payment_time;
```

### 2.3 修改inventory_records表

```sql
ALTER TABLE inventory_records ADD COLUMN operation_datetime DATETIME;
UPDATE inventory_records SET operation_datetime = CONCAT(operation_date, ' ', operation_time);
ALTER TABLE inventory_records DROP COLUMN operation_date, DROP COLUMN operation_time;
```

### 2.4 修改operation_records表

```sql
ALTER TABLE operation_records ADD COLUMN operation_datetime DATETIME;
UPDATE operation_records SET operation_datetime = CONCAT(operation_date, ' ', operation_time);
ALTER TABLE operation_records DROP COLUMN operation_date, DROP COLUMN operation_time;
```

### 2.5 API调整

所有相关API需要调整：
1. 请求参数中使用单一的datetime字段
2. 响应数据中返回datetime字段，同时为了兼容性可以额外返回拆分的date和time字段

## 3. 统计记录设计

### 3.1 系统日志表(system_logs)

为了更好地记录系统操作，提供审计跟踪，设计系统日志表：

| 字段名 | 类型 | 描述 | 约束 |
|--------|------|------|------|
| id | INT | 日志ID | 主键, 自增 |
| log_type | VARCHAR(50) | 日志类型(登录/登出/操作/错误/警告) | 非空 |
| module | VARCHAR(50) | 模块(用户/水果/客户/销售/库存/财务) | 非空 |
| action | VARCHAR(50) | 动作(创建/更新/删除/查询/导出) | 非空 |
| user_id | INT | 用户ID | 外键(users.id) |
| ip_address | VARCHAR(50) | IP地址 | |
| user_agent | VARCHAR(255) | 用户代理 | |
| request_url | VARCHAR(255) | 请求URL | |
| request_method | VARCHAR(10) | 请求方法(GET/POST/PUT/DELETE) | |
| request_params | TEXT | 请求参数(JSON) | |
| response_code | INT | 响应状态码 | |
| response_data | TEXT | 响应数据(JSON) | |
| execution_time | INT | 执行时间(毫秒) | |
| log_datetime | DATETIME | 日志时间 | 非空 |
| created_at | DATETIME | 创建时间 | 非空 |

### 3.2 销售统计表(sales_statistics)

为提高统计查询效率，设计销售统计表，按日/周/月/年预先汇总数据：

| 字段名 | 类型 | 描述 | 约束 |
|--------|------|------|------|
| id | INT | 统计ID | 主键, 自增 |
| statistic_type | VARCHAR(20) | 统计类型(日/周/月/年) | 非空 |
| statistic_date | DATE | 统计日期 | 非空 |
| fruit_id | INT | 水果ID | 外键(fruits.id) |
| customer_id | INT | 客户ID | 外键(customers.id) |
| sales_count | INT | 销售笔数 | 非空, 默认0 |
| sales_quantity | INT | 销售数量 | 非空, 默认0 |
| sales_amount | DECIMAL(12,2) | 销售金额 | 非空, 默认0 |
| paid_amount | DECIMAL(12,2) | 已付款金额 | 非空, 默认0 |
| unpaid_amount | DECIMAL(12,2) | 未付款金额 | 非空, 默认0 |
| created_at | DATETIME | 创建时间 | 非空 |
| updated_at | DATETIME | 更新时间 | 非空 |

## 4. API文档优化

### 4.1 RESTful风格优化

1. 使用HTTP动词表示操作
   - GET: 获取资源
   - POST: 创建资源
   - PUT: 更新资源
   - DELETE: 删除资源

2. 使用复数名词表示资源集合
   - `/fruits` 而非 `/fruit`
   - `/customers` 而非 `/customer`

3. 保持URL结构一致性
   - 集合资源: `/resources`
   - 单个资源: `/resources/{id}`
   - 子资源集合: `/resources/{id}/sub-resources`
   - 子资源: `/resources/{id}/sub-resources/{sub-id}`

### 4.2 新增API

#### 4.2.1 付款与销售记录关联API

- **URL**: `/payments/{id}/sales`
- **方法**: GET
- **描述**: 获取指定付款记录关联的销售记录列表

- **URL**: `/payments/{id}/sales`
- **方法**: POST
- **描述**: 为指定付款记录添加关联的销售记录
- **请求参数**:

```json
{
  "sales": [
    {
      "sales_id": 101,
      "amount": 300.00
    },
    {
      "sales_id": 102,
      "amount": 200.00
    }
  ]
}
```

- **URL**: `/sales/{id}/payments`
- **方法**: GET
- **描述**: 获取指定销售记录关联的付款记录列表

#### 4.2.2 系统日志API

- **URL**: `/logs`
- **方法**: GET
- **描述**: 获取系统日志列表
- **请求参数**: 
  - `page`: 页码
  - `limit`: 每页记录数
  - `log_type`: 日志类型筛选（可选）
  - `module`: 模块筛选（可选）
  - `action`: 动作筛选（可选）
  - `user_id`: 用户ID筛选（可选）
  - `start_datetime`: 开始时间（可选）
  - `end_datetime`: 结束时间（可选）

- **URL**: `/logs/{id}`
- **方法**: GET
- **描述**: 获取指定日志详情

#### 4.2.3 统计分析API

- **URL**: `/statistics/sales`
- **方法**: GET
- **描述**: 获取销售统计数据
- **请求参数**: 
  - `statistic_type`: 统计类型(日/周/月/年)
  - `start_date`: 开始日期
  - `end_date`: 结束日期
  - `fruit_id`: 水果ID筛选（可选）
  - `customer_id`: 客户ID筛选（可选）

- **URL**: `/statistics/inventory`
- **方法**: GET
- **描述**: 获取库存统计数据
- **请求参数**: 
  - `statistic_type`: 统计类型(日/周/月/年)
  - `start_date`: 开始日期
  - `end_date`: 结束日期
  - `fruit_id`: 水果ID筛选（可选）

## 5. 总结

本优化方案主要包括以下几个方面：

1. 创建payment_sales_relation中间表，实现付款记录与销售记录的多对多关系
2. 合并各表中分散的日期和时间字段为单一的datetime字段，提高查询效率
3. 设计系统日志表和销售统计表，加强系统审计和提高统计查询效率
4. 优化API文档，确保完全符合RESTful风格，并新增必要的API接口

这些优化将显著提高系统的数据完整性、查询效率和可维护性。